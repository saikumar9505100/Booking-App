apiVersion: apps/v1
kind: Deployment
metadata:
name: bank-service
namespace: default
spec:
replicas: 2
selector:
matchLabels:
app: bank-service
template:
metadata:
labels:
app: bank-service
spec:
containers:
- name: bank-service
image: YOUR-AWS-ACCOUNT-ID.dkr.ecr.ap-south-1.amazonaws.com/bank-service:latest
ports:
- containerPort: 3000
resources:
requests:
memory: '64Mi'
cpu: '250m'
limits:
memory: '128Mi'
cpu: '500m'
